# PLAN: テキスト差分ビューア

## 1. 概要

2つのテキストを左右のテキストエリアに貼り付けて「比較」ボタンを押すと、行単位の差分（追加・削除・変更）を色分けハイライトで表示する静的な単一ページアプリを作る。バニラJSのみで `public/index.html` 1ファイルに完結させ、サーバ・外部API・ビルドは使わない。

## 2. 意図（明示）

変更前後の文章や設定ファイルの中身を素早く見比べたい人が、その場で貼り付けるだけで差分を確認するための道具。

## 3. 受け入れ条件

テストが直接アサートできるよう、DOM 契約を固定する（実装・テスト共通の前提）:

- 左入力: `#text-a`、右入力: `#text-b`、比較ボタン: `#compare-btn`
- 結果領域: `#result`。差分行は `#result` 直下の `.diff-line` 要素で、種別に応じて `.added` / `.removed` / `.changed` クラスを付与。差分なし行は種別クラスなし
- 状態メッセージ: `#status`（案内・差分件数の表示に使う）

条件（各項目に検証方法を併記。すべて Playwright で `file://` で開いた DOM に対する直接アサート）:

- [ ] **AC1（初期状態）**: 比較実行前は結果領域が空（`#result` 内の `.diff-line` が0件）で、`#status` に案内文（「テキストを入力して比較してください」等）が表示されている
  - テスト: ページを開いた直後に `#result .diff-line` の count が 0、`#status` が可視で案内文を含むことをアサート
- [ ] **AC2（差分の色分け表示）**: 異なる2テキストを貼り付けて比較を実行すると、追加行に `.added`、削除行に `.removed`、変更行に `.changed` が付与されて行単位で色分け表示される
  - テスト: 追加・削除・変更を各1箇所以上含む入力ペアを与え、比較後に `.diff-line.added` / `.diff-line.removed` / `.diff-line.changed` がそれぞれ期待件数・期待テキストで存在することをアサート。さらに各種別1行ずつの computed style（`background-color`）を取得し、3種別が互いに異なり、かつ種別クラスなしの `.diff-line` の背景色とも異なることをアサートして「色分け表示」を直接検証する
- [ ] **AC3（差分0件）**: 同一のテキストを比較すると、差分ハイライト行が0件（`.added` / `.removed` / `.changed` が存在しない）かつ `#status` に差分が0件である旨（「差分はありません」等）が表示される
  - テスト: 同一テキストを両側に入力して比較し、種別クラス付き `.diff-line` の count が 0、`#status` が差分なしの文言を含むことをアサート
- [ ] **AC4（片側空 → 全行追加）**: 左が空で右に複数行の内容がある状態で比較すると、右の全行が `.added` として表示される
  - テスト: 左空・右3行で比較し、`.diff-line.added` の count が 3 で、`.removed` / `.changed` が 0 件であることをアサート
- [ ] **AC5（片側空 → 全行削除）**: 左に複数行の内容があり右が空の状態で比較すると、左の全行が `.removed` として表示される
  - テスト: 左3行・右空で比較し、`.diff-line.removed` の count が 3 で、`.added` / `.changed` が 0 件であることをアサート

すべてのテストを `tests/app.spec.ts` に追記し（既存スモークテストは残す）、`npm test` と `npm run verify` が通ることを完成条件とする。

## 4. 実装方針

`public/index.html` 単一ファイル（CSS/JSインライン、外部リソースなし）。

### レイアウト

- ヘッダー（タイトル）＋2カラムの `<textarea>`（`#text-a` / `#text-b`、ラベル「変更前」「変更後」）＋`#compare-btn`
- その下に `#status`（初期表示は案内文）と `#result`（差分行のリスト、等幅フォント）
- `<head>` に SVG data URI の favicon（左右比較を表す絵柄）をインラインで含める
- footer は AGENTS.md 指定のマークアップで、通常フロー最下部（センタリングコンテナ内の末尾）に置く

### 差分アルゴリズム

行単位の LCS（最長共通部分列）による標準的な diff:

1. `splitLines(text)`: `\n` で分割（末尾の空行の扱いを統一。空文字列は0行として扱う）
2. `computeDiff(aLines, bLines)`: LCS の DP テーブルからバックトラックし、`{type: 'same'|'removed'|'added', text}` の配列を得る
3. 連続する `removed` と `added` のブロックを行数の重なる範囲で `changed` にまとめる（削除直後に追加が続くペアを「変更」と分類）
4. `renderDiff(entries)`: 各エントリを `.diff-line`（＋種別クラス）の `div` として `#result` に描画。テキストは `textContent` で挿入し XSS を避ける
5. 比較後に `#status` を件数表示（差分0件なら「差分はありません」）に更新

入力サイズは貼り付け用途を想定し、LCS は O(n×m) で数千行までは十分。上限を超える巨大入力への最適化は行わない（スコープ外）。

### スタイル

- 追加: 緑系背景、削除: 赤系背景、変更: 黄系背景。色だけに頼らず行頭に `+` / `-` / `~` の記号も表示する
- 差分行は `white-space: pre-wrap` で整形を保持
